# Database - Development (SQLite)
# DATABASE_PROVIDER="sqlite"
# DATABASE_URL="file:./dev.db"

# Database - Production (PostgreSQL on Railway)
DATABASE_PROVIDER="postgresql"
DATABASE_URL="postgresql://user:password@host:port/database?schema=public"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"  # Production: https://your-app.railway.app
NEXTAUTH_SECRET="your-secret-here-use-openssl-rand-base64-32"  # Generate: openssl rand -base64 32

# Node Environment
NODE_ENV="development"  # Use "production" on Railway

# Railway automatically provides:
# - DATABASE_URL (PostgreSQL connection string)
# - PORT (application port)
# - RAILWAY_ENVIRONMENT (production/staging)